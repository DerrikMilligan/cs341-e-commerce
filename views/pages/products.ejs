<%- include('../partials/header.ejs') %>

<div class="container products">
  <div class="row">
    <% products.forEach((product, index) => { %>
      <div class="col-sm-4 p-2">
        <%- include('../components/product.ejs', product) %>
      </div>

      <% if ((index + 1) % 3 === 0) { %>
        </div>
        <div class="row">
      <% } %>
    <% }) %>
  </div>
</div>

<%- include('../partials/footer.ejs') %>
